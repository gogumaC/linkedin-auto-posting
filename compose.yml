
services:
 main:
  build:
   context: .
   dockerfile: main.Dockerfile
  secrets:
    - source: env
      target: .env
 server:
  build:
   context: .
   dockerfile: server.Dockerfile
  secrets:
    - source: env
      target: .env
secrets:
 env:
  file: ./.env